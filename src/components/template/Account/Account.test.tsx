import { render, screen } from '@testing-library/react'
import Account from './Account'

describe('Account molecule', () => {
  
  it('Should be defined', async () => {
    expect(Account).toBeDefined()
  })

})